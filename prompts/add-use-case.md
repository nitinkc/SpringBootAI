# Prompt: Add New Use Case\n\n## Purpose\nCreate a new AI-powered feature following the service layer pattern.\n\n## When to Use\nWhen you want to:\n- Add AI to a new feature (ranking, generation, analysis)\n- Replicate success of existing use cases\n- Experiment with new AI applications\n\n## Prerequisites\n1. Understand existing use cases (Product Search, Support, Recommendations)\n2. Have a clear problem statement\n3. Know expected ROI\n4. Read: `docs/concepts/service-layer-ai.md`\n\n## Step 1: Define the Problem\n\nAnswer these questions:\n- **What** is the user problem? (e.g., \"Search results are generic\")\n- **Why** is it a problem? (e.g., \"Users don't find what they want\")\n- **How** will AI help? (e.g., \"Understand intent better\")\n- **What** is the ROI? (e.g., \"+15% click-through rate\")\n- **What** are the risks? (e.g., \"Wrong recommendations\")\n\n## Step 2: Choose the Pattern\n\nUsing decision matrix:\n\n| Pattern | Input Type | Output Type | Best For |\n|---------|-----------|-------------|----------|\n| Ranking | Query + Items | Sorted list | Search, recommendations |\n| Generation | Context | Text | Support, descriptions |\n| Classification | Record | Label | Sentiment, spam detection |\n| Extraction | Text | Structured data | Review analysis, intent |\n\nExample: \"Review analysis\" = Classification (review → sentiment)\n\n## Step 3: Create Models\n\n**File**: `src/main/java/com/example/aispring/model/[Feature].java`\n\nFollow existing pattern:\n```java\n@Entity\n@Table(name = \"[feature_table]\")\n@Data\n@NoArgsConstructor\n@AllArgsConstructor\npublic class [Feature] {\n    @Id\n    @GeneratedValue\n    private Long id;\n    \n    // Domain attributes\n    private String name;\n    private String data;\n    \n    // AI enrichment\n    private String aiAnalysis;\n    private LocalDateTime aiGeneratedAt;\n}\n```\n\n## Step 4: Create DTOs\n\n**Files**:\n- `src/main/java/com/example/aispring/dto/[Feature]Request.java`\n- `src/main/java/com/example/aispring/dto/[Feature]Response.java`\n\nTemplate:\n```java\n@Data\n@Builder\npublic class [Feature]Request {\n    private String input;        // What AI processes\n    private Map<String, ?> context;  // Additional data\n}\n\n@Data\n@Builder\npublic class [Feature]Response {\n    private [Feature] data;\n    private String aiAnalysis;\n    private boolean aiEnhanced;\n    private Double confidence;   // Optional quality metric\n}\n```\n\n## Step 5: Create Repository\n\n**File**: `src/main/java/com/example/aispring/repository/[Feature]Repository.java`\n\n```java\npublic interface [Feature]Repository extends JpaRepository<[Feature], Long> {\n    List<[Feature]> findByName(String name);\n    // Add domain-specific queries\n}\n```\n\n## Step 6: Create Service\n\n**File**: `src/main/java/com/example/aispring/service/[Feature]Service.java`\n\nCore structure:\n```java\n@Service\npublic class [Feature]Service {\n    \n    private final [Feature]Repository repo;\n    private final AiClient aiClient;\n    private final MeterRegistry metrics;\n    \n    public [Feature]Response process([Feature]Request request) {\n        // Step 1: Validate input\n        validate(request);\n        \n        // Step 2: Fetch related data\n        [Feature] data = fetch(request);\n        \n        // Step 3: Build context for AI\n        String context = buildContext(data, request);\n        \n        // Step 4: Call AI (with fallback)\n        String analysis = null;\n        try {\n            analysis = aiClient.generateResponseWithContext(\n                context,\n                buildPrompt(data)\n            );\n            analysis = validate(analysis);\n        } catch (Exception e) {\n            logger.warn(\"AI failed, using traditional method\", e);\n            analysis = traditionalAnalysis(data);\n        }\n        \n        // Step 5: Enhance data\n        data.setAiAnalysis(analysis);\n        data.setAiGeneratedAt(LocalDateTime.now());\n        repo.save(data);\n        \n        // Step 6: Track metrics\n        metrics.counter(\"[feature].processed\").increment();\n        \n        // Step 7: Return response\n        return buildResponse(data, analysis);\n    }\n    \n    private String buildContext([Feature] data, [Feature]Request request) {\n        return \"\";\n        // Assemble relevant data for prompt\n    }\n    \n    private String buildPrompt([Feature] data) {\n        return \"\";\n        // Create AI prompt\n    }\n}\n```\n\n## Step 7: Create Controller\n\n**File**: `src/main/java/com/example/aispring/controller/[Feature]Controller.java`\n\n```java\n@RestController\n@RequestMapping(\"/api/[feature]\")\npublic class [Feature]Controller {\n    \n    private final [Feature]Service service;\n    \n    @PostMapping\n    public [Feature]Response process(@RequestBody [Feature]Request request) {\n        return service.process(request);\n    }\n}\n```\n\n## Step 8: Write Tests\n\n```java\n@SpringBootTest\nclass [Feature]ServiceTest {\n    \n    @MockBean\n    private AiClient aiClient;\n    \n    @Autowired\n    private [Feature]Service service;\n    \n    @Test\n    void testHappyPath() {\n        when(aiClient.generateResponseWithContext(any(), any()))\n            .thenReturn(\"expected response\");\n        \n        [Feature]Response result = service.process(request);\n        \n        assertNotNull(result);\n        assertTrue(result.isAiEnhanced());\n    }\n    \n    @Test\n    void testFallback() {\n        when(aiClient.generateResponseWithContext(any(), any()))\n            .thenThrow(new TimeoutException());\n        \n        [Feature]Response result = service.process(request);\n        \n        assertNotNull(result);\n        // AI failed, but traditional method was used\n    }\n}\n```\n\n## Step 9: Document the Feature\n\nCreate: `docs/use-cases/[feature].md`\n\nInclude:\n1. Problem statement\n2. Architecture diagram\n3. Cost analysis\n4. Key decisions\n5. Monitoring metrics\n\n## Step 10: Add to Application\n\nUpdate `DataInitializationConfig` with sample data:\n```java\n[Feature] example = [Feature].builder()\n    .name(\"Example\")\n    .build();\nrepo.save(example);\n```\n\n## Checklist\n\n- [ ] Problem defined and ROI estimated\n- [ ] Pattern chosen\n- [ ] Model created with proper annotations\n- [ ] DTOs created (Request + Response)\n- [ ] Repository interface created\n- [ ] Service implemented (7-step pattern)\n- [ ] Controller created\n- [ ] Tests written (>80% coverage)\n- [ ] Manual API testing done\n- [ ] Documentation created\n- [ ] Sample data initialized\n- [ ] Metrics exposed\n\n## Example Use Cases to Add\n\n| Use Case | Type | Complexity | ROI |\n|----------|------|-----------|-----|\n| Review Sentiment | Classification | Low | Medium |\n| Product Description | Generation | Medium | High |\n| Fraud Detection | Classification | High | High |\n| Demand Forecasting | Prediction | High | High |\n| Review Summary | Extraction | Medium | Medium |\n| Price Optimization | Prediction | High | High |\n| Content Moderation | Classification | Medium | High |\n| Chatbot | Generation | High | Medium |\n\n---\n\n**Time estimate**: 4-6 hours for full implementation\n"